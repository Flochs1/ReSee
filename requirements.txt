# Core dependencies
opencv-contrib-python>=4.9.0
numpy>=1.26.0
python-dotenv>=1.0.0
pyyaml>=6.0.1
pydantic>=2.6.0
colorama>=0.4.6

# Gemini API for route planning
google-genai>=1.0.0

# Object detection (CoreML - no PyTorch required at runtime)
coremltools>=7.0

# Voice interface
pyaudio>=0.2.14
SpeechRecognition>=3.10.0
openai-whisper>=20231117

# Optional: Export YOLO to CoreML (one-time, run scripts/export_coreml.py)
# ultralytics>=8.0.0
# torch>=2.0.0
